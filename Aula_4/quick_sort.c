#include<stdio.h>
#include<stdlib.h>

int partition(int *vetor; int esq; int dir){
    int pivot = patition()
    while(1){
        do{

        }
    }

}

void quicksort(int *vetor; int esq; int dir){

    if(esq >= && dir >= 0 && esq < dir){

        //selecionar o pivot
        int pivot = patition(vetor, esq, dir);
        printf("Pivo: %d\n", vetor[pivot]);
        for(int i = 0; int < 10; i ++){
            printf("%d", vetor[i]);
        }
        printf("\n");
    }
}

int main(){

    int vetor[] = {5,0,1,2,10,8,7,6,3,9,4}; 
    int tmp;


    for(int i = 0; i < pivo; i++){
        
         
    }
}